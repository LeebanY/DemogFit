% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihoods.R
\name{wh_sc}
\alias{wh_sc}
\title{Secondary Contact (SC) model likelihood}
\usage{
wh_sc(f, tau1, tau0, theta, k)
}
\arguments{
\item{f}{admixture/migration proportion at the pulse (0 <= f <= 1)}

\item{tau1}{scaled time of the (recent) admixture pulse}

\item{tau0}{scaled ancestral divergence time (tau0 > tau1)}

\item{theta}{scaled mutation rate (per block)}

\item{k}{integer vector of blockwise pairwise-difference counts}
}
\value{
numeric vector, the same length as \code{k}, of
  P(k | f, tau1, tau0, theta) under the SC model
}
\description{
A single, recent pulse of admixture of proportion \code{f} at scaled time
\code{tau1}, following isolation back to the ancestral divergence time
\code{tau0}. Ported from the "pulseADmix" function in the source
notebook. \code{tau1} and \code{tau0} are assumed positive (as enforced
by the default fitting bounds in \code{\link{fit_sc}}).
}
\examples{
wh_sc(f = 0.3, tau1 = 0.2, tau0 = 1.5, theta = 2, k = 0:20)
}
