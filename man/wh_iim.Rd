% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihoods.R
\name{wh_iim}
\alias{wh_iim}
\title{Isolation-with-Initial-Migration (IIM) model likelihood}
\usage{
wh_iim(M, tau1, tau0_total, theta, alpha = 1, k)
}
\arguments{
\item{M}{scaled symmetric migration rate during the ancestral period}

\item{tau1}{recent period of isolation (time since migration ceased)}

\item{tau0_total}{total scaled time back to the ancestral population
merger (must be > \code{tau1}; the migration period itself has
duration \code{tau0_total - tau1})}

\item{theta}{scaled mutation rate (per block)}

\item{alpha}{mutation-rate scalar for a second block/data type; set to 1
when analysing a single block type (the default and typical use case)}

\item{k}{integer vector of blockwise pairwise-difference counts}
}
\value{
numeric vector, the same length as \code{k}, of
  P(k | M, tau1, tau0_total, theta) under the IIM model
}
\description{
Symmetric migration during an ancestral period, ceasing \code{tau1} time
units before the present (isolation for the most recent \code{tau1},
migration for the period stretching back from \code{tau1} to
\code{tau0_total} in the past). Wilkinson-Herbots (2012), eq. 29.
}
\examples{
wh_iim(M = 1.2, tau1 = 0.3, tau0_total = 2, theta = 2, k = 0:20)
}
